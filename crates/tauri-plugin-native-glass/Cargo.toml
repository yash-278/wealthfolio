[package]
name = "tauri-plugin-native-glass"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-native-glass"

[dependencies]
tauri = { version = "2", default-features = false }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
